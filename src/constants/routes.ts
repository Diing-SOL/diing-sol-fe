export enum ROUTES {
  HOME = "/",
  LANDING = "/landing",
  MAIN = "/main",
  RECEIVE = "/receive",
  RECEIVE_WITH_PARAM = "/receive/:token",
  HISTORY = "/history",
  SEND = "/send",
  NOT_FOUND = "/404",
}
